apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "offline-docs.fullname" . }}-test-connection"
  labels:
    {{- include "offline-docs.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "offline-docs.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
