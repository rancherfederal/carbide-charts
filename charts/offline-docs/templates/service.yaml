apiVersion: v1
kind: Service
metadata:
  name: {{ include "offline-docs.fullname" . }}
  labels:
    {{- include "offline-docs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "offline-docs.selectorLabels" . | nindent 4 }}
